# Base URL of the deployed FastAPI backend
# Leave empty in local development to use the Vite proxy,
# or set to the Render backend URL (e.g., https://speakly-backend.onrender.com/api) in production.
VITE_API_URL=
